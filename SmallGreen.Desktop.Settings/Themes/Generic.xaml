<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SmallGreen.Desktop.Settings.BlackControl"
                    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Style x:Key="BucketBaseStyle" TargetType="{x:Type local:ChemicalBucket}">
        <Setter Property="Width" Value="100"/>
        <Setter Property="Height" Value="150"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ChemicalBucket}">
                    <Grid>
                        <!-- Main container -->
                        <Border x:Name="MainBorder"
                                Background="{DynamicResource MaterialDesignPaper}"
                                BorderBrush="{DynamicResource MaterialDesignDivider}"
                                BorderThickness="1"
                                CornerRadius="8">
                            <Grid Margin="4">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Bucket top cap -->
                                <Border Grid.Row="0"
                                        Height="8"
                                        Background="{DynamicResource MaterialDesignDivider}"
                                        CornerRadius="4,4,0,0"
                                        Width="40"
                                        Margin="0,0,0,4"/>

                                <!-- Level value display -->
                                <TextBlock Grid.Row="1"
                                           Text="{Binding LevelValue, RelativeSource={RelativeSource TemplatedParent}, StringFormat={}{0:F0} L}"
                                           HorizontalAlignment="Center"
                                           FontWeight="Bold"
                                           FontSize="14"
                                           Foreground="{DynamicResource MaterialDesignBody}"
                                           Margin="0,4"/>

                                <!-- Liquid container -->
                                <Grid Grid.Row="2" Margin="4,0" x:Name="LiquidHost">
                                    <Border Background="{DynamicResource MaterialDesignDivider}"
                                            Opacity="0.3"
                                            CornerRadius="4"/>

                                    <!-- Liquid fill - simplified without converter for now -->
                                    <Border x:Name="LiquidFill"
                                            Background="{Binding LevelColor, RelativeSource={RelativeSource TemplatedParent}}"
                                            CornerRadius="4"
                                            Opacity="0.7"
                                            VerticalAlignment="Bottom"
                                            Height="50"/>
                                </Grid>

                                <!-- Bucket code -->
                                <TextBlock Grid.Row="3"
                                           Text="{Binding BucketCode, RelativeSource={RelativeSource TemplatedParent}}"
                                           HorizontalAlignment="Center"
                                           FontSize="12"
                                           Foreground="{DynamicResource MaterialDesignBody}"
                                           Margin="0,4,0,2"/>
                            </Grid>
                        </Border>

                        <!-- Selection border -->
                        <Border x:Name="SelectionBorder"
                                BorderBrush="{DynamicResource PrimaryHueMidBrush}"
                                BorderThickness="3"
                                CornerRadius="10"
                                Opacity="0"
                                IsHitTestVisible="False"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="SelectionBorder" Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:ChemicalBucket}" BasedOn="{StaticResource BucketBaseStyle}"/>
</ResourceDictionary>
