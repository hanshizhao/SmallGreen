<prism:PrismApplication x:Class="SmallGreen.Desktop.Settings.App"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:local="clr-namespace:SmallGreen.Desktop.Settings"
                        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                        xmlns:prism="http://prismlibrary.com/"
                        xmlns:converters="clr-namespace:SmallGreen.Desktop.Settings.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!--  合并 MaterialDesign 主题和颜色资源  -->
                <materialDesign:CustomColorTheme BaseTheme="Light"
                                                 ColorAdjustment="{materialDesign:ColorAdjustment}"
                                                 PrimaryColor="#0052b3"
                                                 SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/SmallGreen.Desktop.Settings;component/Assets/Styles/VivaPasswordStyle.xaml" />
                <ResourceDictionary>

                    <!-- Converters -->
                    <converters:LevelToHeightConverter x:Key="LevelToHeightConverter" />
                    <converters:BooleanToInverseConverter x:Key="BooleanToInverseConverter" />
                    <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
                    <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />

                    <!--<converter:TagToIconConverter x:Key="TagToIconConverter" />
                    <converter:ConnectionStatusToColor x:Key="ConnectionStatusToColor" />
                    <converter:ConnectionStatusToMessage x:Key="ConnectionStatusToMessage" />
                    <converter:NullableToVisiblityConverter x:Key="NullableToVisiblityConverter" />
                    <converter:BoolReserveConverter x:Key="BoolReserveConverter" />-->


                    <SolidColorBrush x:Key="ErrorBrush" Color="#d54941" />
                    <SolidColorBrush x:Key="NormalBrush" Color="#0052b3" />
                    <SolidColorBrush x:Key="SuccessBrush" Color="#2ba471" />
                    <!--  DataGrid行中的内容垂直居中  -->
                    <Style x:Key="DataGridCellCenterStyle" TargetType="TextBlock">
                        <Setter Property="TextAlignment" Value="Justify" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                    </Style>

                    <Style x:Key="DrawerTitleStyle" TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{StaticResource NormalBrush}" />
                        <Setter Property="FontSize" Value="20" />
                        <Setter Property="FontWeight" Value="Bold" />
                        <Setter Property="Margin" Value="10,0,0,0" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                    </Style>

                    <Style x:Key="DrawerCloseIconButtonStyle"
                           BasedOn="{StaticResource MaterialDesignIconButton}"
                           TargetType="Button">
                        <Setter Property="Margin" Value="0,0,10,0" />
                        <Setter Property="Foreground" Value="#666" />
                        <Setter Property="HorizontalAlignment" Value="Right" />
                        <Setter Property="Content" Value="{materialDesign:PackIcon Kind=Close, Size=30}" />
                    </Style>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </Application.Resources>
</prism:PrismApplication>
